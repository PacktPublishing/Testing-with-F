namespace chapter10

module _1232OS_10_02 =

    open NUnit.Framework
    
    // don't
    [<Test>]
    let ``store transaction to database`` () = 
        ()

    // do
    [<Test>]
    let ``when customer checkout the cart, the order is persisted`` () =
        ()

